import { Title1 } from "@fluentui/react-components";

/**
 * Profile page placeholder — full implementation in issue #23.
 */
export function ProfilePage(): JSX.Element {
  return (
    <main style={{ maxWidth: "48rem", margin: "0 auto", padding: "2rem 1.5rem" }}>
      <Title1>Your Profile</Title1>
      <p style={{ marginTop: "1rem" }}>Profile page coming in Phase 3.</p>
    </main>
  );
}
