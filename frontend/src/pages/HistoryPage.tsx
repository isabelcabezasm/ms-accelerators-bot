import { Title1 } from "@fluentui/react-components";

/**
 * History page placeholder — full implementation in issue #23.
 */
export function HistoryPage(): JSX.Element {
  return (
    <main style={{ maxWidth: "48rem", margin: "0 auto", padding: "2rem 1.5rem" }}>
      <Title1>Conversation History</Title1>
      <p style={{ marginTop: "1rem" }}>History page coming in Phase 3.</p>
    </main>
  );
}
