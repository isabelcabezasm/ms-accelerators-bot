import { Title1 } from "@fluentui/react-components";

/**
 * Chat page placeholder — full implementation in issue #22.
 */
export function ChatPage(): JSX.Element {
  return (
    <main style={{ maxWidth: "48rem", margin: "0 auto", padding: "2rem 1.5rem" }}>
      <Title1>Chat with the Accelerator Assistant</Title1>
      <p style={{ marginTop: "1rem" }}>Chat interface coming in Phase 3.</p>
    </main>
  );
}
